# SteelVision API (FastAPI). Torch + Ultralytics are reused from the parent repo's
# environment (see ../../requirements.txt) — already installed; not pinned here.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-jose[cryptography]>=3.3
passlib[argon2]>=1.7
python-multipart>=0.0.9
alembic>=1.13
email-validator>=2.1
pillow>=10.0
